// @oxpulse/mesh-core/native — platform detection utilities.
// Re-exported from main index as well; this subpath entry lets consumers
// do: import { isAndroid } from '@oxpulse/mesh-core/native'
export { isInCapacitor, isAndroid, isIOS, isNative } from './platform.js';
